'use strict';

var chai = require('chai');
should = chai.should();
var sinon = require('sinon');
var sinonChai = require('sinon-chai');

chai.use(sinonChai);

//var webpay = require('webpay');

describe('webpay.account', function() {

	describe('.retrieve', function() {

		before(function() {
			// setup
		});

		it('should returns correct object');
		/*
		  , function() {
			webpay.account.retrieve(function(err, res) {
				// response object should be correct
			});
		});
		*/
	});

	describe('.deleteData', function() {
		it('should return true');
		/*
		  , function() {
		  webpay.account.deleteData(function(err, res) {
		  // with no err, res should be true
		  });
		  });
		  */
	});
});
